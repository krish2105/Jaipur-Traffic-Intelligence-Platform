Review the changes in this session against docs/07. Check specifically:
raw plates in logs / event bus / metrics / cache, missing audit-log writes on
P2 access, missing SHAP on any score, unmasked identity by default, retention
gaps, RLS bypass via raw SQL, and any drift toward person-level tracking.
Report findings with file:line. Report PASS explicitly if clean.
