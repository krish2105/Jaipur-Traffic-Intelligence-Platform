Generate a model card for $ARGUMENTS in ml/cards/ per docs/04 §10: purpose,
training data and provenance, metrics with per-condition breakdown (day/night/
rain/dense), limitations, fairness analysis, explicitly excluded features,
intended use and out-of-scope use. Numbers must come from an actual evaluation
run in ml/evaluation/ — never estimated.
